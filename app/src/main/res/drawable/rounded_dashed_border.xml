<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor de fundo do item -->
    <solid android:color="#FFFFFF"/>

    <!-- Cantos arredondados -->
    <corners android:radius="10dp" />

    <!-- Sombra -->
    <stroke
        android:color="#B0BEC5"
        android:width="2dp"/>

    <!-- Padding (sombra de fundo) -->
    <padding
        android:left="5dp"
        android:top="5dp"
        android:right="5dp"
        android:bottom="5dp" />
</shape>
